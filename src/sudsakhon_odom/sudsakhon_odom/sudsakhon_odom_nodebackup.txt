#!/usr/bin/env python3
"""
Robot Bridge Node — ROS2 ↔ STM32 Serial  (merged v3)

Serial format รับจาก STM32:
  POS:curX:curY:curYaw:tick_x:tick_y

Commands ส่งไป STM32:
  G<x>:<y>:<yaw>        → goal position
  V<vx>:<vy>            → manual velocity
  Z                     → reset odometry
  S<speed>              → set max speed
  P5:<rpm>:<rad>:<kp>   → yaw profile
  P6:<kp>:<ki>:<kd>     → position PID
  T<accel>:<decel>:<mv> → trapezoidal profile
"""

"kw">import rclpy
"kw">from rclpy.node "kw">import Node
"kw">import tf2_ros
"kw">import serial
"kw">import threading
"kw">import time
"kw">import math

"kw">from nav_msgs.msg "kw">import Odometry
"kw">from geometry_msgs.msg "kw">import (Quaternion, TransformStamped,
                                Pose2D, Twist,
                                PoseWithCovarianceStamped)
"kw">from std_msgs.msg "kw">import Float32, Empty, Float32MultiArray
"kw">from tf2_ros "kw">import TransformBroadcaster


"kw">class SudsakhonOdomNode(Node):
    "kw">def __init__("kw">self):
        "kw">super().__init__('sudsakhon_odom_node')

        # ── Parameters ───────────────────────────────────────────────────────
        "kw">self.declare_parameter('serial_port', '/dev/Controller_Base')
        "kw">self.declare_parameter('baud_rate',   115200)
        "kw">self.declare_parameter('max_speed',   0.6)

        # ── Serial ───────────────────────────────────────────────────────────
        port      = "kw">self.get_parameter('serial_port').value
        baud_rate = "kw">self.get_parameter('baud_rate').value
        "kw">self.ser  = "kw">None
        "kw">try:
            "kw">self.ser = serial.Serial(port, baud_rate, timeout=0.1)
            time.sleep(1)
            "kw">self.get_logger().info(f"✅ Serial connected: {port}")
            "kw">self.send_to_robot('S', f"{">self.get_parameter('max_speed').value}")
        "kw">except Exception "kw">as e:
            "kw">self.get_logger().error(f"❌ Serial error: {e}")

        # ── Publishers & TF ──────────────────────────────────────────────────
        "kw">self.odom_pub    = "kw">self.create_publisher(Odometry, '/odom', 10)
        "kw">self.tf_br       = TransformBroadcaster("kw">self)

        # ── Subscriptions ─────────────────────────────────────────────────────
        # --- motion commands ---
        "kw">self.create_subscription(Twist,  '/cmd_vel',  "kw">self.vel_callback,  10)
        "kw">self.create_subscription(Pose2D, '/cmd_goal', "kw">self.goal_callback, 10)

        # --- tuner topics ---
        "kw">self.create_subscription(Empty,             '/cmd_reset_odom',   "kw">self.reset_callback,   10)
        "kw">self.create_subscription(Float32MultiArray, '/cmd_pos_pid',      "kw">self.pos_pid_callback, 10)
        "kw">self.create_subscription(Float32MultiArray, '/cmd_yaw_pid',      "kw">self.yaw_pid_callback, 10)
        "kw">self.create_subscription(Float32,           '/cmd_max_speed',    "kw">self.speed_callback,   10)
        "kw">self.create_subscription(Float32MultiArray, '/cmd_trap_profile', "kw">self.trap_callback,    10)

        # --- nav2 init pose (2D Pose Estimate จาก RViz) ---
        "kw">self.create_subscription(PoseWithCovarianceStamped,
                                 '/initialpose', "kw">self.init_pose_cb, 10)

        # ── Odometry state ────────────────────────────────────────────────────
        "kw">self.x   = 0.0
        "kw">self.y   = 0.0
        "kw">self.yaw = 0.0

        # ── Serial read thread ──────────────────────────────────────────────── test1234
        "kw">self.running = "kw">True
        threading.Thread(target="kw">self.serial_loop, daemon="kw">True).start()

        # ── Parameter live-update ─────────────────────────────────────────────
        "kw">self.add_on_set_parameters_callback("kw">self.parameter_callback)

        "kw">self.get_logger().info("SudsakhonOdomNode ready ✅")

    # ── Parameter callback ────────────────────────────────────────────────────
    "kw">def parameter_callback("kw">self, params):
        "kw">for p "kw">in params:
            "kw">if p.name == 'max_speed':
                "kw">self.send_to_robot('S', f"{p.value}")
        "kw">return rclpy.node.SetParametersResult(successful="kw">True)

    # ══════════════════════════════════════════════════════════════════════════
    # TOPIC CALLBACKS
    # ══════════════════════════════════════════════════════════════════════════

    "kw">def vel_callback("kw">self, msg):
        """
        /cmd_vel → V<vx>:<vy>
        ใช้ linear.x / linear.y เป็น velocity ใน robot frame
        angular.z ถูกละเว้น — STM32 ใช้ yaw lock ของตัวเอง
        """
        vx = round(msg.linear.x, 3)
        vy = round(msg.linear.y, 3)
        "kw">self.send_to_robot('V', f"{vx}:{vy}")

    "kw">def goal_callback("kw">self, msg):
        """
        /cmd_goal (Pose2D) → G<x>:<y>:<theta>
        """
        "kw">self.send_to_robot('G', f"{msg.x}:{msg.y}:{msg.theta}")

    "kw">def reset_callback("kw">self, _):
        """
        /cmd_reset_odom → Z
        รีเซ็ต odom ทั้ง STM32 และ state ใน Python
        """
        "kw">self.x = "kw">self.y = 0.0
        "kw">self.send_to_robot('Z', "")
        "kw">self.get_logger().info("Odometry reset")

    "kw">def init_pose_cb("kw">self, msg):
        """
        /initialpose (2D Pose Estimate จาก RViz / Nav2)
        ตั้งค่าตำแหน่งเริ่มต้นโดยไม่ reset STM32
        """
        "kw">self.x = msg.pose.pose.position.x
        "kw">self.y = msg.pose.pose.position.y
        q = msg.pose.pose.orientation
        "kw">self.yaw = math.atan2(
            2.0 * (q.w * q.z + q.x * q.y),
            1.0 - 2.0 * (q.y * q.y + q.z * q.z)
        )
        "kw">self.get_logger().info(
            f"📍 Init pose → X:{">self.x:.3f}  Y:{">self.y:.3f}  θ:{math.degrees(">self.yaw):.1f}°")

    "kw">def pos_pid_callback("kw">self, msg):
        """
        /cmd_pos_pid [kp, ki, kd] → P6:<kp>:<ki>:<kd>
        """
        "kw">if len(msg.data) >= 3:
            kp, ki, kd = msg.data[0], msg.data[1], msg.data[2]
            "kw">self.send_to_robot('P', f"6:{kp}:{ki}:{kd}")

    "kw">def yaw_pid_callback("kw">self, msg):
        """
        /cmd_yaw_pid [max_rpm, brake_rad, fine_kp] → P5:<max_rpm>:<brake_rad>:<fine_kp>
        """
        "kw">if len(msg.data) >= 3:
            rpm, brake, fkp = msg.data[0], msg.data[1], msg.data[2]
            "kw">self.send_to_robot('P', f"5:{rpm}:{brake}:{fkp}")

    "kw">def speed_callback("kw">self, msg):
        """
        /cmd_max_speed (Float32) → S<speed>
        """
        spd = round(float(msg.data), 3)
        "kw">self.send_to_robot('S', f"{spd}")

    "kw">def trap_callback("kw">self, msg):
        """
        /cmd_trap_profile [accel, decel, min_v] → T<accel>:<decel>:<min_v>
        """
        "kw">if len(msg.data) >= 3:
            accel, decel, min_v = msg.data[0], msg.data[1], msg.data[2]
            "kw">self.send_to_robot('T', f"{accel}:{decel}:{min_v}")
            "kw">self.get_logger().info(
                f"Trap profile → accel:{accel:.1f}  decel:{decel:.1f}  min_v:{min_v:.2f}")

    # ══════════════════════════════════════════════════════════════════════════
    # SERIAL SEND
    # ══════════════════════════════════════════════════════════════════════════
    "kw">def send_to_robot("kw">self, cmd_type, data):
        "kw">if "kw">self.ser "kw">and "kw">self.ser.is_open:
            full_cmd = f"{cmd_type}{data}\n"
            "kw">try:
                "kw">self.ser.write(full_cmd.encode())
                if cmd_type != 'V':   # ไม่ log velocity spam
                    "kw">self.get_logger().info(f"TX: {full_cmd.strip()}")
            "kw">except Exception "kw">as e:
                "kw">self.get_logger().error(f"Serial write error: {e}")

    # ══════════════════════════════════════════════════════════════════════════
    # SERIAL READ LOOP
    # STM32 ส่ง: POS:curX:curY:curYaw:tick_x:tick_y
    # ══════════════════════════════════════════════════════════════════════════
    "kw">def serial_loop("kw">self):
        "kw">while "kw">self.running "kw">and rclpy.ok():
            "kw">if "kw">self.ser "kw">and "kw">self.ser.in_waiting > 0:
                "kw">try:
                    line = "kw">self.ser.readline().decode('utf-8', errors='ignore').strip()

                    "kw">if line.startswith("POS:"):
                        parts = line.split(':')
                        "kw">if len(parts) >= 4:
                            x   = float(parts[1])   # curX   (m) — คำนวณแล้วจาก STM32
                            y   = float(parts[2])   # curY   (m)
                            yaw = float(parts[3])   # curYaw (rad)
                            # tick_x = int(parts[4])  # สำรองไว้ถ้าต้องการ
                            # tick_y = int(parts[5])
                            "kw">self._update_pose(x, y, yaw)

                "kw">except Exception "kw">as e:
                    "kw">self.get_logger().debug(f"Serial parse error: {e}")
            time.sleep(0.005)

    "kw">def _update_pose("kw">self, x, y, yaw):
        """อัปเดต pose จาก STM32 แล้ว publish odom + TF"""
        # ถ้า init_pose_cb ตั้งค่าไว้ จะ override ค่าจาก STM32
        # ใช้ค่าจาก STM32 ตรงๆ (STM32 คำนวณ odom เองแล้ว)
        "kw">self.x   = x
        "kw">self.y   = y
        "kw">self.yaw = yaw
        "kw">self.publish_odom(x, y, yaw)

    # ══════════════════════════════════════════════════════════════════════════
    # PUBLISH ODOM + TF
    # ══════════════════════════════════════════════════════════════════════════
    "kw">def publish_odom("kw">self, x, y, yaw):
        now = "kw">self.get_clock().now().to_msg()
        q   = "kw">self._euler_to_quat(yaw)

        # TF: odom → base_link
        t = TransformStamped()
        t.header.stamp        = now
        t.header.frame_id     = 'odom'
        t.child_frame_id      = 'base_link'
        t.transform.translation.x = x
        t.transform.translation.y = y
        t.transform.translation.z = 0.0
        t.transform.rotation      = q
        "kw">self.tf_br.sendTransform(t)

        # Odometry message
        odom = Odometry()
        odom.header.stamp        = now
        odom.header.frame_id     = 'odom'
        odom.child_frame_id      = 'base_link'
        odom.pose.pose.position.x  = x
        odom.pose.pose.position.y  = y
        odom.pose.pose.position.z  = 0.0
        odom.pose.pose.orientation = q
        "kw">self.odom_pub.publish(odom)

    # ══════════════════════════════════════════════════════════════════════════
    # HELPERS
    # ══════════════════════════════════════════════════════════════════════════
    "kw">def _euler_to_quat("kw">self, yaw):
        q = Quaternion()
        q.w = math.cos(yaw * 0.5)
        q.x = 0.0
        q.y = 0.0
        q.z = math.sin(yaw * 0.5)
        "kw">return q

    "kw">def destroy_node("kw">self):
        "kw">self.running = "kw">False
        "kw">if "kw">self.ser "kw">and "kw">self.ser.is_open:
            "kw">self.ser.close()
        "kw">super().destroy_node()


# ══════════════════════════════════════════════════════════════════════════════
"kw">def main(args="kw">None):
    rclpy.init(args=args)
    node = SudsakhonOdomNode()
    "kw">try:
        rclpy.spin(node)
    "kw">except KeyboardInterrupt:
        "kw">pass
    finally:
        node.destroy_node()
        rclpy.shutdown()


"kw">if __name__ == '__main__':
    main()